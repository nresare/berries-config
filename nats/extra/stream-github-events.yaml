apiVersion: jetstream.nats.io/v1beta2
kind: Stream
metadata:
  name: github-events
spec:
  name: github-events
  subjects: ["idcat.github.events.>"]
  storage: file
  maxAge: 1h
  replicas: 3
  account: app
