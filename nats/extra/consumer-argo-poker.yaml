apiVersion: jetstream.nats.io/v1beta2
kind: Consumer
metadata:
  name: argo-poker
spec:
  streamName: github-events
  durableName: argo-poker
  filterSubjects: idcat.github.events.push.portswigger-cloud.manifests
  ackPolicy: explicit
  deliveryPolicy: last
  account: app
